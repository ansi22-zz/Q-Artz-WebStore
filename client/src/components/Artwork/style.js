import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  root: {
    maxWidth: 340,
    marginTop: "100px",
    marginLeft: "9px",
    textDecoration: "none",
  },
  media: {
    height: 200,
  },
});
